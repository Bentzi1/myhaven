# Backend

Node.js API service for the devcontainer workspace.

## Database

Versioned MySQL schema files live under [backend/db](/workspaces/backend/db), with migrations in [backend/db/migrations](/workspaces/backend/db/migrations).

## Scripts

- `npm run dev --workspace backend`
- `npm run start --workspace backend`
